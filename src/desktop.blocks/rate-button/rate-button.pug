mixin rate-button(props)
	form.rate-button
		-let id = Math.random()
		-for (let i = 5; i >= 1; i--)
			input.rate-button__radio(type="radio" name=id id=(id - i) value=i checked=(props.rate === (6 - i)))
			label.rate-button__star(for=(id - i))