include ../checkbox/checkbox

mixin checkbox-group(props)
  div.checkbox-group.js-checkbox-group
    h3.checkbox-group__title.js-checkbox-group__title= props ? props.title : ''
    - for (let i = 0; i < 3; i++)
      div.checkbox-group__checkbox
        +checkbox