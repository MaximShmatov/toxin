mixin checkbox({ caption, name, isChecked } = {})
  label.checkbox
    span.checkbox__caption= caption
    input.checkbox__hidden-input(type="checkbox" name=name checked=isChecked)
    div.checkbox__visible-input